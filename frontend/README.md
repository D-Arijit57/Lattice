# Loom Frontend

Developer-oriented control plane for Loom's backend — manage organizations, content types, schema versions, fields, and entries.

Vite + React 19 + TypeScript + Tailwind + React Router.

## Run locally

**Prerequisites:** Node.js, and the Loom backend running (see `../backend`).

```
npm install
cp .env.example .env
npm run dev
```
