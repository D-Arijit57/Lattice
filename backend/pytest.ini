[pytest]
# Tells pytest-django which settings module to boot Django with.
DJANGO_SETTINGS_MODULE = content_engine.settings
# Which filenames pytest treats as test modules. Default is only
# "test_*.py" / "*_test.py"; we add "tests.py" so app-style names work too.
python_files = test_*.py *_test.py tests.py
